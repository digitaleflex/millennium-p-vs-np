# Datasets

Research datasets and experiment artifacts. Every dataset must document provenance, licensing, generation method and integrity metadata. Store only research data. Large generated datasets should use appropriate external artifact storage when necessary.
